<!DOCTYPE html>
<html>

<head>
    <title>Admin Review: Dogs for Adoption</title>

</head>

<body>
    <h1>Admin Review Give-up Submissions</h1>
    <table>
        <thead>
            <tr>
                <th>Submission ID</th>
                <th>Photo</th>
                <th>Name</th>
                <th>Age</th>
                <th>Status</th>
                <th>Approve</th>
                <th>Details</th>
            </tr>
        </thead>
        <tbody>
            <% if (submissions.length ===0){ %>
                <tr>
                    <td colspan = "7"> No Submissions found</td>
                </tr>
            <% } %>
            <% submissions.forEach(submission => { %>
                <tr>
                    <td><%= submission._id%></td>
                    <td>
                        <% if (submission.photo) { %>
                            <img src = "<%=submission.photo %>" alt="Pet photo">
                            <% } else { %>
                             No photo
                             <% } %>
                    </td>
                    <td><%= submission.dogName %></td>
                    <td><%= submission.age %></td>
                    <td><%= submission.status %></td>
                    <td>
                        <% if (submission.status === "Pending") { %>
                        <form action="/admin/giveups/<%= submission._id %>/approve" method="POST">
                            <button class="action-btn" type="submit">✓</button>
                        </form>

                        <form action="/admin/giveups/<%= submission._id %>/reject" method="POST">
                            <button class="action-btn" type="submit">✗</button>
                        </form>
                        <% } else { %>
                        -
                        <% } %>
                    </td>
                     <td>
                    <a href="/admin/giveups/<%= submission._id %>">view</a>
                    </td>
                </tr>  
                <% }) %>
        </tbody>
    </table>
</body>
</html>